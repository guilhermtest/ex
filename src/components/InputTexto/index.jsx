import './style.css'

export default function InputTexto({ label, id, ...rest }) {
    return (
        <div className="botao-container" {...rest}>
            {nome} {idade}
        </div>
    )
}